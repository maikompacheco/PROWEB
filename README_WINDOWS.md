# Instruções rápidas (Windows)

Abra o PowerShell na pasta do projeto e rode:

```powershell
npm install
npm run dev
```

Se ocorrer problema de permissões, execute o terminal como Administrador ou use `npx`.
